@model List<Alumno>
@* @model IEnumerable<Alumno> *@
@{
    ViewData["Title"] = "Informacion de Alumnos";
    Layout="Simple";
}

<div class="text-center">
    <h1 class="display-4">@ViewData["Title"]</h1>
    
</div>

<partial name="_ListaObjetoEscuela" ></partial>


@* <table class="table table-dark table-hover">
    <thead >
    <tr>
        <th scope="col">Id</th>
        <th scope="col">Nombre</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var alumno in @Model)
    {
        <tr>
            <td>@alumno.UniqueId</td>
            <td>@alumno.Nombre</td>
        </tr>
    }
    </tbody>
</table> *@

@*------Reemplazando Codigo Anterior

@foreach (var alumno in Model)
{
    <partial name="_ObjetoEscuelaSimple" model="alumno" />
} 
*@


<h3>@ViewBag.Fecha</h3>   